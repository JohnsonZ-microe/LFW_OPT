# Core dependencies
# Tested with smtqt conda environment
torch==2.4.1
torchvision==0.19.1
transformers==4.40.0
accelerate>=1.0.0

# Tokenizer (must match transformers==4.40.0)
tokenizers>=0.19,<0.20
huggingface-hub>=0.16.4,<1.0

# Data processing
datasets>=2.12.0
numpy>=1.24.0
pandas>=2.0.0

# Configuration
pyyaml>=6.0
omegaconf>=2.3.0

# Evaluation
scikit-learn>=1.2.0
tqdm>=4.65.0

# Logging and monitoring
tensorboard>=2.13.0
wandb>=0.15.0

# Utilities
matplotlib>=3.7.0
seaborn>=0.12.0

# Install in smtqt conda environment:
# conda activate smtqt
# pip install -r requirements.txt
# Note: If tokenizers version conflict, run:
# find $CONDA_PREFIX/lib/python3.10/site-packages -name 'tokenizers-*.dist-info' -not -name 'tokenizers-0.19*' -exec rm -rf {} +
